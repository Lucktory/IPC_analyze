// ============================================================================
// `components/shared/forms/` — reusable form/modal primitives.
//
// Same registry principle as `lib/shared/`: every form/modal piece lives in
// one place, imported from `@/components/shared/forms`. No duplication.
// ============================================================================

export { Field }         from './Field'
export { SectionHeader } from './SectionHeader'
export { SumPill }       from './SumPill'
export { EntityCombo }   from './EntityCombo'
export { EntityRow }     from './EntityRow'
